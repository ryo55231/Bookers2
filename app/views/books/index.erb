<div class="container">
  <div class="row">

    <div class="col-md-3" style="background-color: orange;">
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


      <!-- 部分テンプレートコード -->
<%= render 'books/list', user: @user, book: @book %>



</div>


 <div class="col-md-8 offset-md-1">
 <h2>Books</h2>
 <table class="table table-striped">
      <tr>
    <th colspan="1"></th>
  　<th>title</th>
　　<th>Opinion</th>
　　</tr>
　　 <% @books.each do |book| %>

　　<tr>
　　<td>
    <%= link_to user_path(current_user.id) do %>
      <%= image_tag  @user.profile_image,size: "100x100" %><!-- ユーザープロフ画像 -->
    <% end %>
    </td>
    <td><%= link_to "title", book_path(book.id) %></td>
    <td><%= book.body %></td>

    </tr>

    <% end %>
    </table>
    </div>
  </div>
</div>